# Phase 1 has no hard dependencies beyond the standard library.
# Uncomment as you build later phases:

# scapy>=2.5.0        # Phase 4: packet capture/analysis
# matplotlib>=3.8.0   # Phase 5/6: charts
# flask>=3.0.0         # Phase 6: web dashboard (if you go that route)
